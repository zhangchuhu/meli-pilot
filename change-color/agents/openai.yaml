interface:
  display_name: "服装批量改色"
  short_description: "用 ImageEye 筛选主 SKU 图库并批量改色"
  default_prompt: "Use $change-color with a product link and a local target-color image. Download candidates through the ImageEye Chrome extension, retain only the verified current-main-SKU gallery, recolor each retained image, and save the results separately."
